#include <stdio.h>

int main(void) {
  int x = 0;

  while( x < 20 ) {
    x++;      
    if( arr[i] % 3 == 0 ) {
      continue; 
    }
    
    arr[j++] = arr[i];
    printf("%d ", arr[i] );
  }

  printf("%d",arr[i] );
  return 0;
}
